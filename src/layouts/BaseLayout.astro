---
import '@fontsource-variable/geist';
import '../styles/global.css';
import type { ImageMetadata } from 'astro';
import Analytics from '@vercel/analytics/astro'
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import fallbackImage from '../assets/site/diego-hero.jpg';

interface Props {
  title: string;
  metaTitle?: string;
  description: string;
  canonicalPath?: string;
  image?: ImageMetadata;
  imageAlt?: string;
  type?: 'website' | 'article';
  publishedAt?: Date;
  updatedAt?: Date;
  structuredData?: Record<string, unknown>;
}

const {
  title,
  metaTitle,
  description,
  canonicalPath = Astro.url.pathname,
  image = fallbackImage,
  imageAlt = 'Diego Amorin, desarrollador web',
  type = 'website',
  publishedAt,
  updatedAt,
  structuredData,
} = Astro.props;
const fullTitle = metaTitle ?? (title === 'Diego Amorin' ? title : `${title} — Diego Amorin`);
const canonical = new URL(canonicalPath, Astro.site);
const socialImage = new URL(image.src, Astro.site);
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="generator" content={Astro.generator} />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="icon" type="image/png" sizes="512x512" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/favicon.png" />
    <link rel="canonical" href={canonical} />
    <link rel="alternate" type="application/rss+xml" title="Blog de Diego Amorin" href="/rss.xml" />

    <meta property="og:locale" content="es_PE" />
    <meta property="og:type" content={type} />
    <meta property="og:site_name" content="Diego Amorin" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta property="og:image:alt" content={imageAlt} />
    {publishedAt && <meta property="article:published_time" content={publishedAt.toISOString()} />}
    {updatedAt && <meta property="article:modified_time" content={updatedAt.toISOString()} />}

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImage} />
    {structuredData && <script is:inline type="application/ld+json" set:html={JSON.stringify(structuredData)} />}
    <Analytics />
  </head>
  <body>
    <a class="skip-link" href="#main-content">Ir al contenido</a>
    <Header />
    <main id="main-content">
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style>
  .skip-link {
    position: fixed;
    top: 0.75rem;
    left: 0.75rem;
    z-index: 1000;
    padding: 0.65rem 0.9rem;
    transform: translateY(-150%);
    border-radius: 6px;
    background: #111;
    color: #fff;
    transition: transform 160ms ease;
  }

  .skip-link:focus {
    transform: translateY(0);
  }
</style>
